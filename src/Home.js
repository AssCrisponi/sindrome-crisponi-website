import React from 'react';
import Header from './Header';
import MainWrapper from './MainWrapper';

const Home = () => {
  return (
    <div>
      <header>
        <Header />
      </header>
    <MainWrapper />
    </div>
  );
}

export default Home;